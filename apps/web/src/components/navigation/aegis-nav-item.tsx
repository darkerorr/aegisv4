"use client";
import Link from "next/link";import type{LucideIcon}from"lucide-react";import{Tooltip,TooltipContent,TooltipTrigger}from"@/components/ui/tooltip";
export function AegisNavItem({href,icon:Icon,label,accent="neutral",active,collapsed=false}:{href:string;icon:LucideIcon;label:string;accent?:"neutral"|"cyan"|"blue"|"violet"|"green"|"yellow"|"orange"|"red";active:boolean;collapsed?:boolean}){const item=<Link href={href} aria-current={active?"page":undefined} aria-label={collapsed?label:undefined} data-active={active} className="workspace-nav-item icon-action focus-ring" style={{"--icon-color":`var(--${accent==="neutral"?"foreground-soft":accent})`} as React.CSSProperties}><Icon aria-hidden="true" size={18} strokeWidth={1.8} absoluteStrokeWidth/><span>{label}</span></Link>;return <Tooltip><TooltipTrigger asChild>{item}</TooltipTrigger><TooltipContent side="right">{label}</TooltipContent></Tooltip>}
