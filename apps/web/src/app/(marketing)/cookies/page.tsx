import { Cookie } from "lucide-react";
import { LegalPageShell } from "@/components/legal/legal-page-shell";
import { getLegalConfig } from "@/lib/legal/config";

export const metadata = { title: "Cookies" };
export default function CookiesPage() { const legal = getLegalConfig(); return <LegalPageShell eyebrow="Cookie policy" title="Necessary by design." intro="The audited Web path currently uses one authentication cookie and no analytics or marketing SDK." icon={Cookie} configured={legal.configured}><section><h2>Strictly necessary</h2><div className="cookie-record"><div><span>Name</span><strong>aegis_session</strong></div><div><span>Purpose</span><strong>Authenticated session</strong></div><div><span>Attributes</span><strong>HttpOnly · SameSite=Lax · Path=/</strong></div><div><span>Security</span><strong>Secure in production</strong></div><div><span>Duration</span><strong>{legal.retention.sessions ? `${legal.retention.sessions} days` : "Controlled by API session configuration; public value not configured"}</strong></div></div></section><section><h2>Preferences, analytics and marketing</h2><p>No non-essential analytics or marketing tracker was found in the audited Web code. A consent banner is therefore intentionally not displayed. If such a tracker is added, it must remain disabled before consent and offer equally accessible accept and reject actions.</p></section><section><h2>Your controls</h2><p>Signing out clears the Aegis session cookie. Browser-level cookie removal can also end the local session. Necessary authentication cannot operate while its cookie is blocked.</p></section></LegalPageShell>; }
