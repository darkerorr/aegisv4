import { ShieldCheck } from "lucide-react";
import { LegalPageShell } from "@/components/legal/legal-page-shell";
import { getLegalConfig } from "@/lib/legal/config";

export const metadata = { title: "Security" };
export default function SecurityPage() { const legal = getLegalConfig(); return <LegalPageShell eyebrow="Security" title="Controls, not slogans." intro="Observed technical measures and reporting status, without presenting them as a certification or absolute guarantee." icon={ShieldCheck} configured={legal.configured}><section><h2>Observed controls</h2><div className="security-measures"><div><strong>Session cookies</strong><p>HttpOnly, SameSite=Lax, scoped to the application path and Secure in production.</p></div><div><strong>Passwords</strong><p>The API stores password hashes rather than raw passwords.</p></div><div><strong>Provider and integration secrets</strong><p>Encryption is used where the corresponding server encryption key is configured.</p></div><div><strong>Session review</strong><p>Authenticated sessions can be listed and revoked from account security.</p></div></div></section><section><h2>Security reporting</h2><p>{legal.identity.email ? <>Report suspected vulnerabilities privately to <a href={`mailto:${legal.identity.email}`}>{legal.identity.email}</a>. Do not include live credentials or personal data.</> : "A dedicated security reporting address has not been configured. Public launch should not proceed without one."}</p></section><section><h2>Not a certification</h2><p>No penetration test, ISO certification, SOC report or independent security audit is represented by this page.</p></section></LegalPageShell>; }
