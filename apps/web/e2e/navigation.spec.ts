import{test,expect}from"@playwright/test";import{mockApi}from"./helpers";test("workspace navigation",async({page})=>{await mockApi(page);await page.goto("/chat");await expect(page.getByRole("complementary",{name:"Workspace navigation"})).toBeVisible();await page.getByRole("link",{name:"Providers",exact:true}).click();await expect(page.getByRole("heading",{name:"Providers"})).toBeVisible()})