import {test,expect}from"@playwright/test";import{mockApi}from"./helpers";test("reduced motion and keyboard access",async({page})=>{await mockApi(page,{auth:"anonymous"});await page.emulateMedia({reducedMotion:"reduce"});await page.goto("/");await page.keyboard.press("Tab");await expect(page.getByText("Skip to content")).toBeFocused();await expect(page.locator("html")).toHaveCSS("scroll-behavior","auto")})
