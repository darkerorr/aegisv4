import {test,expect}from"@playwright/test";import{mockApi}from"./helpers";test("real conversation history opens and reloads",async({page})=>{await mockApi(page);await page.goto("/chat");await page.getByText("Launch narrative",{exact:true}).click();await expect(page).toHaveURL(/conv-1/);await page.reload();await expect(page.locator("#main").getByText("Lead with protected choice, not model count.")).toBeVisible()})
