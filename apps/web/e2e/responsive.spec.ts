import {test,expect}from"@playwright/test";import{mockApi}from"./helpers";test("mobile landing has no horizontal overflow",async({page})=>{await mockApi(page,{auth:"anonymous"});await page.setViewportSize({width:390,height:844});await page.goto("/");await expect(page.getByRole("button",{name:"Open menu"})).toBeVisible();const overflow=await page.evaluate(()=>document.documentElement.scrollWidth>document.documentElement.clientWidth);expect(overflow).toBe(false);await page.screenshot({path:"test-results/screenshots/landing-mobile.png"})})
