import {test,expect}from"@playwright/test";import{mockApi}from"./helpers";
test.beforeEach(async({page})=>mockApi(page,{auth:"anonymous"}));test("login and register render and submit",async({page})=>{await page.goto("/login");await page.screenshot({path:"test-results/screenshots/login.png"});await page.getByLabel("Email address").fill("design@aegis.local");await page.getByLabel("Password",{exact:true}).fill("correct-horse");await page.getByRole("button",{name:"Sign in"}).click();await expect(page).toHaveURL(/chat/);await page.goto("/register");await expect(page.getByRole("heading",{name:"Think on your terms."})).toBeVisible()})
