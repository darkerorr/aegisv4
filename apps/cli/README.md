# Aegis CLI workspace facade

The existing CLI remains the compatibility implementation at the repository
root while the shared packages are extracted. The workspace package preserves
the `aegis` binary path for future standalone publishing.
